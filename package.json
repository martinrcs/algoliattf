{
  "name": "iff-indexer",
  "type": "module",
  "private": true,
  "scripts": {
    "index": "node index.mjs",
    "pw:install": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "algoliasearch": "^5.7.0",
    "playwright": "^1.48.2"
  }
}
